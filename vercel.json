{
  "buildCommand": "npm run build --workspace=packages/shared --if-present && npm run build --workspace=apps/web --if-present",
  "outputDirectory": "apps/web/dist",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": null
}
