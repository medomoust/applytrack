{
  "buildCommand": "cd ../.. && npm ci && npm run build --workspace=packages/shared && cd apps/web && npm run build",
  "outputDirectory": "apps/web/dist",
  "installCommand": "npm install",
  "framework": "vite"
}
