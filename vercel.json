{
  "buildCommand": "cd packages/shared && npm install && npm run build && cd ../.. && cd apps/web && npm install && npm run build && cd ../..",
  "outputDirectory": "apps/web/dist",
  "installCommand": "npm install",
  "framework": null
}
